[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "psoc6-ecg-personalization"
version = "0.1.0"
description = "TinyML ECG last-layer personalization on PSoC 6"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
